------------------------------------------------------------------comparaison entre flask et spring boot-------------------------------------------------------------------------

1)Avec Flask (ce que tu connais)
	1. Tu crées un dossier manuellement
	2. Tu crées app.py manuellement
	3. Tu installes les libs avec pip
	4. Tu écris tout from scratch

2)Avec Spring Boot (nouveau)
	1. Tu vas sur start.spring.io
	2. Tu remplis un formulaire
	3. Tu cliques Generate
	4. Il te donne un projet complet prêt à l'emploi

	💡 Spring Boot génère automatiquement toute la structure du projet — tu n'as rien à créer manuellement !


3)C'est quoi start.spring.io ?

	C'est le générateur officiel de projets Spring Boot — comme un formulaire IKEA.
		Tu remplis ce que tu veux
      			  ↓
		Il génère un fichier .zip
      			  ↓
		Tu extrais → projet prêt ✅

4)Maven  → utilise pom.xml    (comme pip avec requirements.txt)

5)Packaging — Jar ou War
	Jar → application autonome ✅ (comme un .exe)
	War → nécessite un serveur externe (ancien)
	
	On choisit Jar — plus simple, Spring Boot a son propre serveur intégré

6)Avec JPA/Hibernate tu écris juste ça ?
	
	JPA = Java Persistence API
		C'est une interface standard qui dit comment Java doit communiquer avec une base de données.

	// AVEC JPA — une seule ligne ✅
		userRepository.save(user);
```

		> 💡 Hibernate génère automatiquement le SQL pour toi !

---

		## La différence JPA vs Hibernate
```
		JPA       = le PATRON qui donne les ordres
		Hibernate = l'EMPLOYÉ qui exécute les ordres
```

	Plus concrètement :
```
		JPA dit :
		"Il faut une méthode save() pour sauvegarder"
		"Il faut une méthode findAll() pour récupérer"
		"Il faut une méthode delete() pour supprimer"
              	↓
		Hibernate dit :
		"OK je vais créer ces méthodes"
		"Pour save() je vais écrire INSERT INTO..."
		"Pour findAll() je vais écrire SELECT * FROM..."
		"Pour delete() je vais écrire DELETE FROM..."

7)C'est quoi Lombok ?

	AVEC Lombok — simple et propre ✅
		@Getter
		@Setter
	public class User { ... }

	💡 Lombok c'est un **raccourci** qui génère automatiquement le code répétitif Java(il definit auto les getter et setter)



8)
	Sans start.spring.io ❌        Avec start.spring.io ✅
                               
	Tu crées tout manuellement     Il génère tout automatiquement
	Tu configures Maven toi-même   pom.xml déjà configuré
	Tu crées les dossiers          Structure complète prête
	Tu configures Spring           Configuration de base incluse
	   ⏱️ 2-3 heures                  ⏱️ 30 secondes

9)
	Artifact = ask-enset
              ↓
Nom du projet = ask-enset
Nom du fichier final = ask-enset.jar

	Ton code Java
      ↓
	Maven compile
      ↓
	ask-enset.jar  ← tout en un seul fichier
      ↓
	Tu lances avec :
	java -jar ask-enset.jar

-------------------------------------------------------------configuration et installation des dependencies depuis https://start.spring.io/-------------------------------------

1)remplir les champs

2)Dépendances à ajouter :
	
	✅ Spring Web //Créer les API REST de ton projet
	✅ Spring Data JPA//Communiquer avec PostgreSQL sans écrire de SQL
	✅ PostgreSQL Driver//Le pont de connexion entre Spring Boot et PostgreSQL
	✅ Spring Security //Gérer tout ce qui concerne la sécurité et l'authentification
	✅ Lombok // Éviter le code répétitif Java
	✅ Spring Boot DevTools //Redémarrer automatiquement le serveur après chaque modification


	Sans DevTools ❌               Avec DevTools ✅

	Tu modifies le code             Tu modifies le code
        ↓                               ↓
	Tu arrêtes le serveur           Serveur redémarre
        ↓                                 automatiquement ✅
	Tu relances le serveur
        ↓
	Tu attends 30 secondes

3)Ce qui se passe:
	vscode lit le pom.xml
        ↓
Il voit les dépendances :
  - Spring Web
  - Spring Data JPA
  - PostgreSQL Driver
  - Spring Security
  - Lombok
  - DevTools
        ↓
Maven les télécharge automatiquement
depuis Maven Central
        ↓
Tout est prêt ✅

4)Structure complète du projet ask-enset :

ask-enset/
│
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── ma/enset/askEnset/
│   │   │           └── AskEnsetApplication.java
│   │   └── resources/
│   │           ├── application.properties
│   │           ├── static/
│   │           └── templates/
│   └── test/
│           └── java/
│                   └── ma/enset/askEnset/
│                           └── AskEnsetApplicationTests.java
│
├── pom.xml
├── mvnw
├── mvnw.cmd
└── .gitignore

5)## 🎯 Résumé visuel

ask-enset/
│
├── AskEnsetApplication.java  → Lance l'application
├── pom.xml                   → Liste des dépendances
├── application.properties    → Configuration (DB, Redis, AI)
├── src/main/java/            → Ton code Java
├── src/main/resources/       → Ressources et config
├── src/test/                 → Tests automatiques
├── mvnw.cmd                  → Lance Maven sur Windows
└── .gitignore                → Fichiers ignorés par Git


6) AskEnsetApplication.java:C'est le point d'entrée de toute l'application — le premier fichier qui s'exécute.C'est comme app.py en Flask — c'est lui qui lance tout !
7)application.properties:C'est le fichier de configuration de toute l'application. C'est comme le fichier .env en Flask — toute la configuration au même endroit !
8)pom.xml:C'est la liste de toutes les dépendances du projet.
9)src/main/resources/:C'est là où tu mets les fichiers de configuration et ressources.
10)src/test/:C'est là où tu écris les tests automatiques de ton application.

11)mvnw(Lance Maven sur Linux/Mac) et mvnw.cmd(Lance Maven sur Windows):

--------------------------------------------------------------------configuration de db-----------------------------------------------------------------------------

1)spring.datasource.url=jdbc:postgresql://pg-7768e65-imgharnenourddine3-59b8.l.aivencloud.com:26613/ask_enset?sslmode=require #L'adresse complète de ta base de données :
spring.datasource.username=avnadmin
spring.datasource.password=TON_MOT_DE_PASSE
spring.datasource.driver-class-name=org.postgresql.Driver #Dit à Spring Boot d'utiliser le driver PostgreSQL pour se connecter

spring.jpa.hibernate.ddl-auto=update #Dit à Hibernate quoi faire avec les tables :update → crée les tables si elles n'existent pas, met à jour si elles changent ✅
spring.jpa.show-sql=true #Affiche dans la console le SQL généré par Hibernate — utile pour débugger
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect #Dit à Hibernate qu'on utilise PostgreSQL spécifiquement — il adapte le SQL en conséquence

server.port=8080 #C'est le numéro de porte sur lequel ton application Spring Boot va écouter.


---------------------------------------------------------------------creation des modeles de db--------------------------------------------------------------------------

1)src/main/java/ma/enset/askEnset/
        └── model/
                ├── User.java
                ├── Conversation.java
                ├── Message.java
                └── Document.java

2) SDK (Software Development Kit)
C'est le Java que tu as installé sur ton PC — IntelliJ a besoin de savoir lequel utiliser.

3) Language Level
C'est la version des fonctionnalités Java que tu autorises dans ton code.


-------------------------------------------------------------------user table---------------------------------------------------------------------------------

1)package ma.enset.ask_enset.model;



import jakarta.persistence.*;//c'est le package JPA
import lombok.*;//c'est le package Lombok
import java.time.LocalDateTime;

@Entity//Dit à Hibernate "cette classe est une table" dans la base de données
@Table(name = "users")//Dit à Hibernate que le nom de la table dans PostgreSQL sera users
@Getter//Génère automatiquement tous les getters :
@Setter//Génère automatiquement tous les setters :
@NoArgsConstructor//Génère un constructeur vide automatiquement :
@AllArgsConstructor//Génère un constructeur avec tous les attributs :
public class User {

    @Id//Dit que cet attribut est la clé primaire de la table
    @GeneratedValue(strategy = GenerationType.IDENTITY)//L'id est généré automatiquement par PostgreSQL (1, 2, 3...)
    private Long id;

    @Column(nullable = false)
    private String nom;

    @Column(nullable = false)
    private String prenom;

    @Column(nullable = false, unique = true)
    private String email;

    @Column(nullable = false)
    private String password;
	
    @OneToMany(mappedBy = "user", cascade = CascadeType.ALL)/Une user → contient → Plusieurs conversation //user supprimée->Tous les conversation supprimés automatiquement ✅
    private List<Conversation> conversations;//si en faire user.getConversations() il nous donne les conversation

    @Enumerated(EnumType.STRING)/Ça dit à Hibernate comment stocker l'enum dans PostgreSQL ,stocke "ETUDIANT" ou "ADMIN" 
    @Column(nullable = false)
    private Role role;/`Role` → c'est le **type de données** (comme String, Integer...)

    @Column(name = "image_path")//le nom de la colonne dans PostgreSQL sera image_path
    private String imagePath = "profiles/default.jpg";

    @Column(name = "created_at")
    private LocalDateTime createdAt;

    @Column(name = "updated_at")
    private LocalDateTime updatedAt;

    @PrePersist//exécuté **automatiquement** juste avant la création d'un utilisateur
    protected void onCreate() {
        createdAt = LocalDateTime.now();
        updatedAt = LocalDateTime.now();
    }

    @PreUpdate//exécuté **automatiquement** quand tu modifies un utilisateur
    protected void onUpdate() {
        updatedAt = LocalDateTime.now();
    }

    public enum Role {/ enum type spécial (liste de valeurs fixes),Role     → nom du type
        ETUDIANT, ADMIN
    }
}

----------------------------------------------------------------------conversation table-----------------------------------------------------------------------------

1)package ma.enset.ask_enset.model;



import jakarta.persistence.*;
import lombok.*;
import java.time.LocalDateTime;
import java.util.List;

@Entity
@Table(name = "conversations")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class Conversation {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne //Plusieurs conversations → appartiennent à → Un seul user
    @JoinColumn(name = "user_id", nullable = false)//@JoinColumn → crée la colonne user_id dans la table 
    private User user;//private User user → l'objet User complet (pas juste l'id) conversation.getUser().getNom()  // → "Alami"

    @Column(nullable = false)
    private String titre;

    @OneToMany(mappedBy = "conversation", cascade = CascadeType.ALL)//Une conversation → contient → Plusieurs messages ;//mappedBy = "conversation" → dit à JPA que la relation est déjà définie dans Message.java :
    private List<Message> messages;

    @Column(name = "created_at")
    private LocalDateTime createdAt;

    @Column(name = "updated_at")
    private LocalDateTime updatedAt;

    @PrePersist
    protected void onCreate() {
        createdAt = LocalDateTime.now();
        updatedAt = LocalDateTime.now();
    }

    @PreUpdate
    protected void onUpdate() {
        updatedAt = LocalDateTime.now();
    }
}


-----------------------------------------------------------------------message table ----------------------------------------------------------------------------------

1)package ma.enset.ask_enset.model;



import jakarta.persistence.*;
import lombok.*;
import java.time.LocalDateTime;

@Entity
@Table(name = "messages")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class Message {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @ManyToOne
    @JoinColumn(name = "conversation_id", nullable = false)
    private Conversation conversation;

    @Column(nullable = false, columnDefinition = "TEXT")//VARCHAR → limité à 255 caractères;TEXT → illimité ✅ — parfait pour les réponses du chatbot qui peuvent être longues
    private String contenu;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private TypeMessage type;

    @Column(name = "created_at")
    private LocalDateTime createdAt;

    @PrePersist
    protected void onCreate() {
        createdAt = LocalDateTime.now();
    }

    public enum TypeMessage {
        USER, BOT
    }
}


-----------------------------------------------------------------------------------document table------------------------------------------------------------------

1)package ma.enset.askEnset.model;

import jakarta.persistence.*;
import lombok.*;
import java.time.LocalDateTime;

@Entity
@Table(name = "documents")
@Getter
@Setter
@NoArgsConstructor
@AllArgsConstructor
public class Document {

    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;

    @Column(name = "nom_fichier", nullable = false)
    private String nomFichier;

    @Column(nullable = false)
    private String description;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private Categorie categorie;

    @Column(name = "chemin_minio", nullable = false)
    private String cheminMinio;

    @Enumerated(EnumType.STRING)
    @Column(nullable = false)
    private Statut statut = Statut.EN_ATTENTE;//Quand un PDF est uploadé → statut automatiquement `EN_ATTENTE` :

    @ManyToOne
    @JoinColumn(name = "uploaded_by", nullable = false)
    private User uploadedBy;

    @Column(name = "created_at")
    private LocalDateTime createdAt;

    @PrePersist
    protected void onCreate() {
        createdAt = LocalDateTime.now();
    }

    public enum Categorie {
        EMPLOI_DU_TEMPS, EXAMEN, REGLEMENT, STAGE
    }

    public enum Statut {
        EN_ATTENTE, INDEXE, ERREUR
    }
}

2)le processus de traitement des documents :
1. Admin uploade PDF
        ↓
2. Spring Boot sauvegarde dans MinIO
   chemin = "examens/calendrier_examens.pdf"
        ↓
3. LangChain4j récupère le PDF depuis MinIO
   via cheminMinio
        ↓
4. LangChain4j extrait le texte du PDF
        ↓
5. LangChain4j découpe le texte en chunks :
   Chunk 1 : "Les examens du S1 auront lieu..."
   Chunk 2 : "Le calendrier est le suivant..."
   Chunk 3 : "Les rattrapages sont prévus..."
        ↓
6. OpenAI convertit chaque chunk en vecteur
        ↓
7. Vecteurs stockés dans Qdrant ✅
        ↓
8. statut = INDEXE ✅























